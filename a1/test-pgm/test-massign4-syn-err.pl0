var
  x: int;
  y: int;
begin
  x : = y;           // erroneous blank between : and =
  x := y || y := x;  // error || should be |
  x := y & y := x;   // error & should be |
  x := y | while true do y:=1  // a while statement isn't an assignment
end
  